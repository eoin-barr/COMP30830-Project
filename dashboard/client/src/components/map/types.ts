export interface MarkerType {
  name: string;
  lat: number;
  lng: number;
}

export interface StationType {
  address: string;
  banking: boolean;
  bonus: boolean;
  lat: string;
  lng: string;
  name: string;
  number: string;
}
