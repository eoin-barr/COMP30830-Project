export interface MarkerType {
  name: string;
  lat: number;
  lng: number;
}

export interface StationType {
  lat: string;
  lng: string;
  name: string;
  bonus: boolean;
  number: string;
  address: string;
  banking: boolean;
}
